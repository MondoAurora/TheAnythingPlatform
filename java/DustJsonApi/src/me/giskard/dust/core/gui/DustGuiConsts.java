package me.giskard.dust.core.gui;

import me.giskard.dust.core.DustConsts;

public interface DustGuiConsts extends DustConsts {
    String TOKEN_GUI_MAIN = UNIT_GUI + DUST_SEP_TOKEN + "guiMain";
    String TOKEN_GUI_FOCUS = UNIT_GUI + DUST_SEP_TOKEN + "guiFocus";
    String TOKEN_GUI_TOP_MENU = UNIT_GUI + DUST_SEP_TOKEN + "guiTopMenu";
    String TOKEN_GUI_PROGRESS = UNIT_GUI + DUST_SEP_TOKEN + "guiProgress";

  	String TOKEN_GUI_LOOKANDFEEL = UNIT_GUI + DUST_SEP_TOKEN + "lookAndFeel";

}
